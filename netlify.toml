[build]
  publish = "static"

# Redirect /static/* to root (HTML refs /static/css/... for local dev compat)
[[redirects]]
  from = "/static/*"
  to = "/:splat"
  status = 200

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
